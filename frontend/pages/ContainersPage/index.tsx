import Header from '@components/Header';
import Card from '@components/Card';

export default function ContainersPage(){
	return (
		<>
			<Header />
			<main className="container">
				<Card title="Containers">
					Trang demo danh sách container (đang phát triển...)
				</Card>
			</main>
		</>
	);
}
