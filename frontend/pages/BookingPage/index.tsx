import Header from '@components/Header';
import Card from '@components/Card';

export default function BookingPage(){
	return (
		<>
			<Header />
			<main className="container">
				<Card title="Booking">
					Trang demo đặt container (đang phát triển...)
				</Card>
			</main>
		</>
	);
}
